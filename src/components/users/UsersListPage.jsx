import React, { useEffect, useState } from 'react';
import { usersService } from '../../services/api'; function UsersListPage() { const [users, setUsers] = useState([]); const [loading, setLoading] = useState(true); const [error, setError] = useState(''); useEffect(() => { let isMounted = true; const loadUsers = async () => { try { const data = await usersService.getUsers(); if (isMounted) { setUsers(Array.isArray(data) ? data : []); } } catch (err) { if (isMounted) { setError(err.message || 'Failed to fetch users.'); } } finally { if (isMounted) { setLoading(false); } } }; loadUsers(); return () => { isMounted = false; }; }, []); return ( <main className="mx-auto w-full max-w-3xl px-4 py-10"> <h1 className="mb-6 text-3xl font-bold text-gray-900">Users</h1> {loading && <p className="text-gray-600">Loading users...</p>} {!loading && error && ( <p className="rounded-md border border-red-200 bg-red-50 p-3 text-red-700"> {error} </p> )} {!loading && !error && users.length === 0 && ( <p className="text-gray-600">No users found.</p> )} {!loading && !error && users.length > 0 && ( <ul className="space-y-3"> {users.map((user) => ( <li key={user._id} className="rounded-lg border border-gray-200 bg-white p-4 shadow-sm" > <p className="font-semibold text-gray-900">{user.username}</p> <p className="text-sm text-gray-600">{user.email}</p> <p className="mt-1 text-xs uppercase tracking-wide text-gray-500"> Role: {user.role} </p> </li> ))} </ul> )} </main> );
} export default UsersListPage;
